<?php

  session_start();

  function protectPage($type) {
    // if (!isset($_SESSION['user_type']) || $_SESSION['user_type'] !== $type) {
    //   header('Location: login.php');
    //   exit;
    // }
}